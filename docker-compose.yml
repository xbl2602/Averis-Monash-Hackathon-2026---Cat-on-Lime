# Start locally with a single command: docker compose up --build
# First copy .env.example to .env and fill in real keys (see README.md)
services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
