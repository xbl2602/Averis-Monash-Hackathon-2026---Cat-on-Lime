# dependencies
/node_modules

# next.js build output
/.next/
/out/
next-env.d.ts

# production build
/build

# env files — real secrets live only in these files; do not commit them
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# TypeScript build info
*.tsbuildinfo

# Private research materials and orchestration logs — never added to git, committed, or pushed
/private/
/.paizhang/

# The perturbation test set is generated output (rebuilt by scripts/perturb-generate.mjs)
/data/perturb/

# Python cache (produced by running score_cli.py locally)
__pycache__/
*.pyc
